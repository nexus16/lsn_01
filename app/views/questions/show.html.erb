<div>
  <div class="container-question">
    <%= render @question %>
  </div>
  <% if current_user && policy(Report).destroy? %>
    <div class="container-manager-reports">
      <%= render "list_reports", question: @question %>
    </div>
  <% end %>

  <div class="list-answers">
    <div class="count-answer">
      <h5>
        <span><%= @supports_question.answers.count %></span>
        <%= t "answer_count" %>
      </h5>
    </div>
    <div class="divide"></div>
    <div class="container-answers">
      <% @supports_question.answers.roots.each_with_index do |answer, index| %>
        <%= render answer, vote: @supports_question.vote_answers[index] %>
      <% end %>
    </div>
  </div>
</div>
